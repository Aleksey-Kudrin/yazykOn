import { createHmac } from "node:crypto";
import { execFileSync } from "node:child_process";
import { test, expect, type Page } from "@playwright/test";
const primary=process.env.SFU_PRIMARY_URL??"http://127.0.0.1:4100",secondary=process.env.SFU_SECONDARY_URL??"http://127.0.0.1:4200",compose=process.env.SFU_FAILOVER_COMPOSE??"e2e/docker-compose.sfu-failover.yml",redisUrl=process.env.REDIS_URL??"redis://127.0.0.1:6379",secret=process.env.ROOM_ACCESS_SECRET??"integration-secret",ttl=Number(process.env.SFU_ROOM_OWNER_TTL_MS??3000),rooms=Math.min(4,Math.max(2,Number(process.env.SFU_ISOLATION_ROOMS??3))),members=3;
const token=(roomId:string,userId:string)=>{const p=Buffer.from(JSON.stringify({roomId,userId,role:"participant",exp:Math.floor(Date.now()/1000)+900})).toString("base64url");return p+"."+createHmac("sha256",secret).update(p).digest("base64url")};
async function redis(roomIds:string[]){const {createClient}=await import("redis");const c=createClient({url:redisUrl});await c.connect();const out=await Promise.all(roomIds.map(async roomId=>({roomId,owner:await c.get("yazykon:sfu:owner:"+roomId),peers:await c.keys("yazykon:sfu:peer:"+roomId+":")})));await c.quit();return out}
async function connect(page:Page,endpoint:string,roomId:string,userId:string,peerId=""){return page.evaluate(async({endpoint,roomId,accessToken,peerId})=>{const s:any=globalThis.__iso??={stream:undefined,ws:undefined,pc:undefined};s.ws?.close();s.pc?.close();s.stream??=await navigator.mediaDevices.getUserMedia({video:true,audio:false});const pc=new RTCPeerConnection();s.stream.getTracks().forEach((t:MediaStreamTrack)=>pc.addTrack(t,s.stream));const ws=new WebSocket(endpoint.replace(/^http/,"ws")+"/ws");const q:any[]=[];const w=new Map<string,((x:any)=>void)[]>();const wait=(t:string)=>new Promise<any>((res,rej)=>{const tm=setTimeout(()=>rej(Error("timeout "+t)),12000);const i=q.findIndex(x=>x.type===t);if(i>=0){clearTimeout(tm);return res(q.splice(i,1)[0])}const a=w.get(t)??[];a.push(x=>{clearTimeout(tm);res(x)});w.set(t,a)});ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.type==="ice"&&m.data)void pc.addIceCandidate(m.data);const a=w.get(m.type);if(a?.length)a.shift()!(m);else q.push(m)};await new Promise<void>((res,rej)=>{ws.onopen=()=>res();ws.onerror=()=>rej(Error("ws"))});pc.onicecandidate=e=>{if(e.candidate&&ws.readyState===1)ws.send(JSON.stringify({type:"ice",roomId,data:e.candidate.toJSON()}))};ws.send(JSON.stringify({type:"join",roomId,data:{accessToken,reconnect:!!peerId,...peerId?{peerId}:{}}}));const j=await wait("joined");const o=await pc.createOffer();await pc.setLocalDescription(o);ws.send(JSON.stringify({type:"offer",roomId,data:pc.localDescription}));const a=await wait("answer");if(a.data)await pc.setRemoteDescription(a.data);s.ws=ws;s.pc=pc;return j.peerId},{endpoint,roomId,accessToken:token(roomId,userId),peerId})}
test("SFU rooms remain isolated during asymmetric churn",async({browser})=>{test.skip(!process.env.SFU_FAILOVER_LIVE,"Set SFU_FAILOVER_LIVE=1");const ids=Array.from({length:rooms},(_,i)=>"ISO-"+Date.now()+"-"+i);const people:any[]=[];const pages:Page[]=[];try{for(const roomId of ids)for(let i=0;i<members;i++){const page=await browser.newPage({permissions:["camera","microphone"]});await page.goto(primary+"/health");pages.push(page);people.push({page,roomId,userId:roomId+"-u-"+i,peerId:""})}const initial=await Promise.all(people.map(p=>connect(p.page,primary,p.roomId,p.userId)));initial.forEach((r,i)=>people[i].peerId=r);expect((await redis(ids)).every(x=>x.peers.length===members)).toBeTruthy();execFileSync("docker",["compose","-f",compose,"stop","sfu-primary"],{stdio:"inherit"});await new Promise(r=>setTimeout(r,ttl+700));expect((await redis(ids)).every(x=>JSON.parse(x.owner??"{}").nodeId==="integration-secondary")).toBeTruthy();const subset=people.filter((_,i)=>i%2===0);const rec=await Promise.all(subset.map(p=>connect(p.page,secondary,p.roomId,p.userId,p.peerId)));rec.forEach((r,i)=>expect(r).toBe(subset[i].peerId));execFileSync("docker",["compose","-f",compose,"start","sfu-primary"],{stdio:"inherit"});await new Promise(r=>setTimeout(r,500));execFileSync("docker",["compose","-f",compose,"stop","sfu-secondary"],{stdio:"inherit"});await new Promise(r=>setTimeout(r,ttl+700));const reverse=await redis(ids);expect(reverse.every(x=>JSON.parse(x.owner??"{}").nodeId==="integration-primary")).toBeTruthy();const all=await Promise.all(people.map(p=>connect(p.page,primary,p.roomId,p.userId,p.peerId)));all.forEach((r,i)=>expect(r).toBe(people[i].peerId));const final=await redis(ids);expect(final.every(x=>x.peers.length===members)).toBeTruthy()}finally{try{execFileSync("docker",["compose","-f",compose,"start","redis","sfu-primary","sfu-secondary"],{stdio:"inherit"})}catch{}await Promise.all(pages.map(p=>p.close()))}});