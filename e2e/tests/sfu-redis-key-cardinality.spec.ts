import { test, expect } from "@playwright/test";
const redisUrl=process.env.REDIS_URL??"redis://127.0.0.1:6379",endpoint=process.env.SFU_PRIMARY_URL??"http://127.0.0.1:4100";
test("Redis SFU key families stay bounded",async()=>{test.skip(!process.env.SFU_FAILOVER_LIVE,"Set SFU_FAILOVER_LIVE=1");const {createClient}=await import("redis");const c=createClient({url:redisUrl});await c.connect();const families=["owner","state","tracks","peer"];const before=Object.fromEntries(await Promise.all(families.map(async f=>[f,(await c.keys("yazykon:sfu:"+f+":*")).length])));for(let i=0;i<5;i++){await fetch(endpoint+"/health");await new Promise(r=>setTimeout(r,250))}const after=Object.fromEntries(await Promise.all(families.map(async f=>[f,(await c.keys("yazykon:sfu:"+f+":*")).length])));for(const f of families)expect(after[f as keyof typeof after]).toBeLessThanOrEqual(before[f as keyof typeof before]+Number(process.env.SFU_MAX_IDLE_REDIS_KEYS??20));await c.quit()});
